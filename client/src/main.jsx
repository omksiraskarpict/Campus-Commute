import React, { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { BrowserRouter, Link, Navigate, Route, Routes, useLocation, useNavigate } from 'react-router-dom';
import { Activity, AlertTriangle, Bell, CalendarDays, CarFront, CheckCircle2, ChevronRight, Clock3, Compass, Database, Filter, Home, LogOut, MapPin, Menu, MessageCircle, Plus, Search, Settings, ShieldCheck, Sparkles, UserRound, Users, X } from 'lucide-react';
import './styles.css';
import { AdminTools, StudentApp } from './features.jsx';
import { AdminRoutes } from './admin/routes/AdminRoutes.jsx';

const API = import.meta.env.VITE_API_URL || (import.meta.env.DEV ? 'http://localhost:5000/api' : 'https://campus-commute.onrender.com/api');
const getToken = () => localStorage.getItem('campus-token');
async function request(path, options = {}) {
  let response;
  try {
    response = await fetch(`${API}${path}`, { ...options, headers: { 'Content-Type': 'application/json', ...(getToken() ? { Authorization: `Bearer ${getToken()}` } : {}), ...options.headers } });
  } catch {
    throw new Error('API unavailable. Start the server with npm run dev, then check port 5000.');
  }
  const body = await response.json().catch(() => ({}));
  if (!response.ok) {
    const error = new Error(body.message || 'Request failed');
    error.status = response.status;
    throw error;
  }
  return body;
}

function App() {
  const [user, setUser] = useState(null);
  const [screen, setScreen] = useState('home');
  const [authMode, setAuthMode] = useState('login');
  const [showCreate, setShowCreate] = useState(false);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    if (getToken()) {
      request('/auth/me')
        .then(({ user: account }) => setUser(account))
        .catch(() => localStorage.removeItem('campus-token'))
        .finally(() => setLoading(false));
      return;
    }
    setLoading(false);
  }, []);

  const logout = () => {
    localStorage.removeItem('campus-token');
    setUser(null);
  };

  const requireUser = (element) => user ? element : <Navigate to="/login" replace />;
  const requireAdmin = (element) => user && user.role === 'ADMIN' ? element : <Navigate to="/admin/login" replace />;

  if (loading) return <div className="loading-screen"><div className="logo-mark">CC</div><span>Getting your commute ready...</span></div>;

  return (
    <Routes>
      <Route path="/" element={
        !user
          ? (authMode === 'welcome' ? <Welcome onStart={() => setAuthMode('login')} /> : <Auth mode={authMode} onMode={setAuthMode} onLogin={(account) => setUser(account)} />)
          : (user.role === 'ADMIN' ? <Navigate to="/admin/dashboard" replace /> : <Navigate to="/dashboard" replace />)
      } />

      <Route path="/login" element={
        user ? (user.role === 'ADMIN' ? <Navigate to="/admin/dashboard" replace /> : <Navigate to="/dashboard" replace />) : <Auth mode="login" onMode={setAuthMode} onLogin={(account) => setUser(account)} />
      } />

      <Route path="/register" element={
        user ? (user.role === 'ADMIN' ? <Navigate to="/admin/dashboard" replace /> : <Navigate to="/dashboard" replace />) : <Auth mode="register" onMode={setAuthMode} onLogin={(account) => setUser(account)} />
      } />

      <Route path="/dashboard" element={requireUser(<StudentApp request={request} user={user} setUser={setUser} onLogout={logout} />)} />
      <Route path="/rides" element={requireUser(<StudentApp request={request} user={user} setUser={setUser} onLogout={logout} />)} />
      <Route path="/bookings" element={requireUser(<StudentApp request={request} user={user} setUser={setUser} onLogout={logout} />)} />
      <Route path="/chat" element={requireUser(<StudentApp request={request} user={user} setUser={setUser} onLogout={logout} />)} />
      <Route path="/profile" element={requireUser(<StudentApp request={request} user={user} setUser={setUser} onLogout={logout} />)} />

      <Route path="/admin/*" element={<AdminRoutes user={user} setUser={setUser} onLogout={logout} request={request} />} />

      <Route path="*" element={<Navigate to={user ? (user.role === 'ADMIN' ? '/admin/dashboard' : '/dashboard') : '/'} replace />} />
    </Routes>
  );
}

function Welcome({ onStart }) { return <main className="welcome"><div className="welcome-nav"><div className="brand"><span className="logo-mark">CC</span><span>Campus <b>Commute</b></span></div><button className="text-button" onClick={onStart}>Sign in <ChevronRight size={16} /></button></div><section className="hero"><div className="hero-copy"><p className="eyebrow"><Sparkles size={15} /> Built for campus life</p><h1>Better rides.<br /><em>Closer community.</em></h1><p className="hero-subtitle">Find your people on the way to class. Share a seat, split the cost, and make every commute feel a little lighter.</p><button className="primary-button" onClick={onStart}>Start commuting <ChevronRight size={18} /></button><div className="hero-proof"><div className="avatar-stack"><span>AR</span><span>NS</span><span>MK</span><span>+</span></div><p><strong>2,400+</strong><br />students already moving together</p></div></div><div className="hero-art"><div className="art-sun"></div><div className="art-route route-one"></div><div className="art-route route-two"></div><div className="art-card"><MapPin size={18} /><span>PICT Main Gate</span><b>8:10 AM</b></div><div className="art-car"><CarFront size={82} strokeWidth={1.2} /></div><div className="art-note"><ShieldCheck size={19} /><div><b>Verified students</b><small>Travel with confidence</small></div></div></div></section></main> }

function Auth({ mode, onMode, onLogin }) { const navigate = useNavigate(); const location = useLocation(); const [form, setForm] = useState({ name: '', email: '', password: '' }); const [confirmPassword, setConfirmPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); const submit = async (event) => { event.preventDefault(); setBusy(true); setError(''); if (mode === 'register' && form.password !== confirmPassword) { setError('Passwords do not match.'); setBusy(false); return; } try { const data = await request(`/auth/${mode === 'login' ? 'login' : 'register'}`, { method: 'POST', body: JSON.stringify(form) }); if (mode === 'register') { localStorage.removeItem('campus-token'); navigate('/login', { replace: true, state: { registrationSuccess: true } }); return; } localStorage.setItem('campus-token', data.token); onLogin(data.user); } catch (e) { setError(e.message); } finally { setBusy(false); } }; const registrationSuccess = mode === 'login' && location.state?.registrationSuccess; return <main className="auth-page"><div className="auth-visual"><div className="brand light"><span className="logo-mark">CC</span><span>Campus <b>Commute</b></span></div><div className="auth-message"><p className="eyebrow">YOUR CAMPUS, CONNECTED</p><h1>Go together.<br /><em>Go further.</em></h1><p>Simple, safe, student-powered rides for the moments between where you are and where you need to be.</p></div><div className="auth-orbit orbit-one"></div><div className="auth-orbit orbit-two"></div></div><div className="auth-panel"><button className="mobile-brand brand" onClick={() => onMode('welcome')}><span className="logo-mark">CC</span><span>Campus <b>Commute</b></span></button><div className="auth-heading"><p className="eyebrow">{mode === 'login' ? 'WELCOME BACK' : 'JOIN THE MOVEMENT'}</p><h2>{mode === 'login' ? 'Your next ride is closer.' : 'Make your commute count.'}</h2><p>{mode === 'login' ? 'Sign in to see what is happening on your route.' : 'Create a verified student account in under a minute.'}</p></div>{registrationSuccess && <div className="success-box">Account created successfully. Please sign in.</div>}<form onSubmit={submit} className="auth-form">{mode !== 'login' && <label>Full name<input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Your name" required /></label>}<label>College email<input type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="you@college.edu" required /></label><label>Password<input type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="At least 8 characters" required minLength="8" /></label>{mode !== 'login' && <label>Confirm password<input type="password" value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} placeholder="Re-enter your password" required minLength="8" /></label>}{error && <div className="error-box">{error}</div>}<button className="primary-button full" disabled={busy}>{busy ? 'Please wait...' : mode === 'login' ? 'Sign in' : 'Create account'} <ChevronRight size={18} /></button></form>{mode === 'login' && <Link className="student-login-admin-link" to="/admin/login">Admin Panel</Link>}<p className="switch-auth">{mode === 'login' ? 'New to Campus Commute?' : 'Already have an account?'} <Link to={mode === 'login' ? '/register' : '/login'}>{mode === 'login' ? 'Create an account' : 'Sign in'}</Link></p></div></main> }

function StudentsView({ onBack }) {
  const [students, setStudents] = useState([]);
  const [query, setQuery] = useState('');
  const [filter, setFilter] = useState('ALL');
  const [selected, setSelected] = useState(null);
  const [loading, setLoading] = useState(true);
  const [busyId, setBusyId] = useState('');
  const [error, setError] = useState('');
  const [notice, setNotice] = useState('');
  const load = () => { setLoading(true); setError(''); request('/admin/students?limit=100').then(({ students: items }) => setStudents(items)).catch((e) => setError(e.message)).finally(() => setLoading(false)); };
  useEffect(load, []);
  const filtered = students.filter((student) => {
    const matchesQuery = `${student.name} ${student.email}`.toLowerCase().includes(query.toLowerCase());
    const matchesFilter = filter === 'ALL' || student.role === filter || student.verificationStatus === filter || student.accountStatus === filter;
    return matchesQuery && matchesFilter;
  });
  const updateStudent = async (student, path, body, message) => { setBusyId(student._id); setError(''); setNotice(''); try { const result = await request(`/admin/students/${student._id}/${path}`, { method: 'PATCH', body: JSON.stringify(body) }); setStudents((items) => items.map((item) => item._id === student._id ? result.student : item)); setSelected(result.student); setNotice(message); } catch (e) { setError(e.message); } finally { setBusyId(''); } };
  const statusClass = (value) => value === 'VERIFIED' || value === 'ACTIVE' ? 'status verified-status' : 'status pending-status';
  return <main className="admin-main students-main"><header className="admin-header students-header"><div><button className="link-button admin-back" onClick={onBack}><ChevronRight size={16} />Overview</button><p className="eyebrow">PEOPLE DIRECTORY</p><h1>Manage <em>students.</em></h1><p className="muted">Review accounts, verification, and community access.</p></div><div className="admin-status"><span></span> Admin only</div></header>{notice && <div className="success-box"><CheckCircle2 size={17} />{notice}</div>}{error && <div className="error-box">{error}</div>}<section className="student-toolbar"><div className="student-search"><Search size={18} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search by name or email" /></div><div className="student-filters"><Filter size={16} />{['ALL', 'VERIFIED', 'PENDING', 'ADMIN', 'ACTIVE', 'SUSPENDED'].map((value) => <button className={filter === value ? 'filter-button active' : 'filter-button'} key={value} onClick={() => setFilter(value)}>{value[0] + value.slice(1).toLowerCase()}</button>)}</div></section><section className="admin-panel students-panel"><div className="admin-panel-head"><div><p className="eyebrow">{filtered.length} RESULTS</p><h2>Student accounts</h2></div><button className="soft-button" onClick={load} disabled={loading}><Activity size={16} />Refresh</button></div>{loading ? <div className="admin-loading">Loading students...</div> : filtered.length === 0 ? <div className="empty-admin"><Users size={26} /><h3>No students found</h3><p>Try a different search or filter.</p></div> : <div className="students-table"><div className="student-table-head"><span>Student</span><span>Verification</span><span>Account</span><span>Joined</span><span>Role</span><span>Actions</span></div>{filtered.map((student) => <button className="student-row" key={student._id} onClick={() => setSelected(student)}><div className="student-identity"><div className="avatar small">{student.name.slice(0, 2).toUpperCase()}</div><span><b>{student.name}</b><small>{student.email}</small></span></div><span className={statusClass(student.verificationStatus)}>{student.verificationStatus}</span><span className={statusClass(student.accountStatus)}>{student.accountStatus || 'ACTIVE'}</span><small>{new Date(student.createdAt).toLocaleDateString()}</small><span className="role-label">{student.role}</span><span className="row-action">View <ChevronRight size={15} /></span></button>)}</div>}</section>{selected && <div className="modal-backdrop" onMouseDown={(event) => event.target === event.currentTarget && setSelected(null)}><section className="modal student-modal"><div className="modal-head"><div><p className="eyebrow">STUDENT PROFILE</p><h2>{selected.name}</h2></div><button className="icon-button" onClick={() => setSelected(null)}><X /></button></div><div className="student-detail-grid"><div><small>Email</small><b>{selected.email}</b></div><div><small>Role</small><b>{selected.role}</b></div><div><small>Verification</small><span className={statusClass(selected.verificationStatus)}>{selected.verificationStatus}</span></div><div><small>Account</small><span className={statusClass(selected.accountStatus)}>{selected.accountStatus || 'ACTIVE'}</span></div><div><small>Joined</small><b>{new Date(selected.createdAt).toLocaleDateString()}</b></div><div><small>College</small><b>{selected.college || 'Not provided'}</b></div><div><small>Department</small><b>{selected.department || 'Not provided'}</b></div><div><small>Year</small><b>{selected.year || 'Not provided'}</b></div><div><small>Phone</small><b>{selected.phone || 'Not provided'}</b></div><div><small>Rating</small><b>{selected.rating?.toFixed?.(1) || '0.0'} ({selected.completedRides || 0} rides)</b></div></div><div className="student-actions">{selected.verificationStatus !== 'VERIFIED' && <button className="primary-button" disabled={busyId === selected._id} onClick={() => updateStudent(selected, 'verify', {}, 'Student verified successfully.')}>{busyId === selected._id ? 'Saving...' : 'Verify student'}</button>}{selected.role !== 'ADMIN' && <button className="soft-button" disabled={busyId === selected._id} onClick={() => updateStudent(selected, 'status', { accountStatus: selected.accountStatus === 'SUSPENDED' ? 'ACTIVE' : 'SUSPENDED' }, `Student ${selected.accountStatus === 'SUSPENDED' ? 'activated' : 'suspended'} successfully.`)}>{selected.accountStatus === 'SUSPENDED' ? 'Activate account' : 'Suspend account'}</button>}</div></section></div>}</main>;
}

function AdminDashboard({ user, onLogout }) {
  const [section, setSection] = useState('overview');
  const tool = section === 'rides' || section === 'reports' || section === 'audit';
  return section === 'students' ? <div className="admin-shell"><AdminSidebar user={user} onLogout={onLogout} onStudents={() => setSection('students')} onSection={setSection} active={section} /><StudentsView onBack={() => setSection('overview')} /></div> : tool ? <div className="admin-shell"><AdminSidebar user={user} onLogout={onLogout} onStudents={() => setSection('students')} onSection={setSection} active={section} /><AdminTools request={request} section={section} onBack={() => setSection('overview')} /></div> : <AdminOverview user={user} onLogout={onLogout} onStudents={() => setSection('students')} onSection={setSection} />;
}

function AdminSidebar({ user, onLogout, onStudents, onSection, active }) {
  return <aside className="admin-sidebar"><div className="brand"><span className="logo-mark">CC</span><span>Campus <b>Commute</b></span></div><div className="admin-badge"><ShieldCheck size={15} /> Admin control centre</div><p className="nav-label">MONITORING</p><button className={active === 'overview' ? 'nav-item active' : 'nav-item'} onClick={() => onSection('overview')}><Activity size={19} />Overview</button><button className={active === 'students' ? 'nav-item active' : 'nav-item'} onClick={onStudents}><Users size={19} />Students</button><button className={active === 'rides' ? 'nav-item active' : 'nav-item'} onClick={() => onSection('rides')}><CarFront size={19} />All rides</button><button className={active === 'reports' ? 'nav-item active' : 'nav-item'} onClick={() => onSection('reports')}><AlertTriangle size={19} />Reports</button><button className={active === 'audit' ? 'nav-item active' : 'nav-item'} onClick={() => onSection('audit')}><Database size={19} />Audit logs</button><div className="sidebar-bottom"><div className="mini-profile"><div className="avatar">{user.name.slice(0, 2).toUpperCase()}</div><div><b>{user.name}</b><small>Platform administrator</small></div><button onClick={onLogout} title="Log out"><LogOut size={17} /></button></div></div></aside>;
}

function AdminOverview({ user, onLogout, onStudents, onSection }) {
  const [metrics, setMetrics] = useState(null);
  const [users, setUsers] = useState([]);
  const [error, setError] = useState('');
  useEffect(() => { const handleClick = (event) => { const label = event.target.closest('.admin-sidebar .nav-item')?.textContent?.trim(); if (label === 'Students') onStudents(); if (label === 'All rides') onSection('rides'); if (label === 'Reports') onSection('reports'); if (label === 'Audit logs') onSection('audit'); }; document.addEventListener('click', handleClick); return () => document.removeEventListener('click', handleClick); }, [onStudents, onSection]);
  useEffect(() => { Promise.all([request('/admin/overview'), request('/admin/users')]).then(([overview, list]) => { setMetrics(overview.metrics); setUsers(list.users); }).catch((e) => setError(e.message)); }, []);
  const cards = metrics ? [['Registered students', metrics.users, Users], ['Verified community', metrics.verified, ShieldCheck], ['Active rides', metrics.active, Activity], ['Completed rides', metrics.completed, CarFront], ['Pending verification', metrics.pending, AlertTriangle], ['Cancelled rides', metrics.cancelled, CalendarDays]] : [];
  return <div className="admin-shell"><aside className="admin-sidebar"><div className="brand"><span className="logo-mark">CC</span><span>Campus <b>Commute</b></span></div><div className="admin-badge"><ShieldCheck size={15} /> Admin control centre</div><p className="nav-label">MONITORING</p><button className="nav-item active"><Activity size={19} />Overview</button><button className="nav-item"><Users size={19} />Students</button><button className="nav-item"><CarFront size={19} />All rides</button><button className="nav-item"><AlertTriangle size={19} />Reports</button><button className="nav-item"><Database size={19} />Audit logs</button><div className="sidebar-bottom"><div className="mini-profile"><div className="avatar">{user.name.slice(0, 2).toUpperCase()}</div><div><b>{user.name}</b><small>Platform administrator</small></div><button onClick={onLogout} title="Log out"><LogOut size={17} /></button></div></div></aside><main className="admin-main"><header className="admin-header"><div><p className="eyebrow">LIVE PLATFORM MONITOR</p><h1>Good morning, <em>{user.name.split(' ')[0]}.</em></h1><p className="muted">A clear view of how Campus Commute is moving today.</p></div><div className="admin-status"><span></span> All systems operational</div></header>{error && <div className="error-box">{error}</div>}<section className="admin-metrics">{cards.length ? cards.map(([label, value, Icon]) => <div className="admin-metric" key={label}><div className="admin-metric-icon"><Icon size={19} /></div><span>{label}</span><strong>{value}</strong><small>{label === 'Pending verification' ? 'Needs review' : 'Across the platform'}</small></div>) : <div className="admin-loading">Loading live platform metrics...</div>}</section><section className="admin-grid"><div className="admin-panel"><div className="admin-panel-head"><div><p className="eyebrow">RECENT ACTIVITY</p><h2>Latest students</h2></div><button className="link-button">View all <ChevronRight size={16} /></button></div><div className="user-table">{users.map((student) => <div className="user-row" key={student._id}><div className="avatar small">{student.name.slice(0, 2).toUpperCase()}</div><div><b>{student.name}</b><small>{student.email}</small></div><span className={student.verificationStatus === 'VERIFIED' ? 'status verified-status' : 'status pending-status'}>{student.verificationStatus}</span><small className="date-cell">{new Date(student.createdAt).toLocaleDateString()}</small></div>)}</div></div><div className="admin-panel health-panel"><div className="admin-panel-head"><div><p className="eyebrow">SYSTEM HEALTH</p><h2>Platform pulse</h2></div><Activity size={19} color="#62a66b" /></div><div className="health-line"><span><Database size={16} />MongoDB</span><b>Connected</b></div><div className="health-line"><span><Activity size={16} />API service</span><b>Healthy</b></div><div className="health-line"><span><MessageCircle size={16} />Live messaging</span><b>Online</b></div><div className="health-summary"><strong>{metrics?.active || 0}</strong><span>rides currently moving through the platform</span></div></div></section></main></div>
}

function Dashboard({ user, screen, setScreen, onLogout, showCreate, setShowCreate }) { const [rides, setRides] = useState([]); const [notifications, setNotifications] = useState([]); const [query, setQuery] = useState(''); const [mobileNav, setMobileNav] = useState(false); const load = () => { request(`/rides?source=${encodeURIComponent(query)}`).then(({ rides: items }) => setRides(items)).catch(() => {}); request('/notifications').then(({ notifications: items }) => setNotifications(items)).catch(() => {}); }; useEffect(load, [query]); const join = (id) => request(`/rides/${id}/join`, { method: 'POST' }).then(load).catch((e) => window.alert(e.message)); return <div className="app-shell"><aside className={mobileNav ? 'sidebar open' : 'sidebar'}><div className="brand"><span className="logo-mark">CC</span><span>Campus <b>Commute</b></span></div><button className="close-nav" onClick={() => setMobileNav(false)}><X /></button><p className="nav-label">WORKSPACE</p><nav>{[[Home, 'Home', 'home'], [Search, 'Find a ride', 'search'], [CalendarDays, 'My trips', 'trips'], [MessageCircle, 'Messages', 'messages']].map(([Icon, label, value]) => <button className={screen === value ? 'nav-item active' : 'nav-item'} key={value} onClick={() => { setScreen(value); setMobileNav(false); }}><Icon size={19} />{label}{value === 'messages' && <span className="nav-count">2</span>}</button>)}</nav><p className="nav-label second">ACCOUNT</p><nav><button className="nav-item"><Bell size={19} />Notifications{notifications.some((item) => !item.read) && <span className="notification-dot" />}</button><button className="nav-item"><UserRound size={19} />Profile</button><button className="nav-item"><Settings size={19} />Settings</button></nav><div className="sidebar-bottom"><div className="mini-profile"><div className="avatar">{user.name.slice(0, 2).toUpperCase()}</div><div><b>{user.name}</b><small>Student account</small></div><button onClick={onLogout} title="Log out"><LogOut size={17} /></button></div><div className="verified"><ShieldCheck size={16} /> Verified community</div></div></aside><main className="main-content"><header className="topbar"><button className="menu-button" onClick={() => setMobileNav(true)}><Menu /></button><div className="search-box"><Search size={18} /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search your next ride..." /></div><div className="top-actions"><button className="icon-button notification-button"><Bell size={20} />{notifications.some((item) => !item.read) && <span />}</button><div className="top-user"><div className="avatar">{user.name.slice(0, 2).toUpperCase()}</div><div><b>{user.name}</b><small>{user.college || 'Campus student'}</small></div></div></div></header>{screen === 'home' || screen === 'search' ? <><section className="welcome-row"><div><p className="eyebrow">{screen === 'home' ? new Date().toLocaleDateString(undefined, { weekday: 'long', day: 'numeric', month: 'long' }).toUpperCase() : 'EXPLORE YOUR ROUTE'}</p><h1>{screen === 'home' ? <>Good morning, <em>{user.name.split(' ')[0]}.</em></> : <>Find a ride that <em>fits.</em></>}</h1><p className="muted">{screen === 'home' ? 'Your campus commute, thoughtfully connected.' : 'Search by pickup, destination, date, or time.'}</p></div><div className="quick-actions"><button className="soft-button" onClick={() => setScreen('search')}><Search size={17} />Find a ride</button><button className="primary-button" onClick={() => setShowCreate(true)}><Plus size={18} />Offer a ride</button></div></section><section className="stats-row"><div className="stat-card highlight"><div className="stat-icon"><Compass size={20} /></div><div><span>Your commute</span><strong>Feels lighter together</strong></div><ChevronRight size={18} /></div><div className="stat-card"><span>Upcoming trips</span><strong>03</strong><small>this month</small></div><div className="stat-card"><span>Time saved</span><strong>4.5h</strong><small>by sharing rides</small></div></section><section className="section-heading"><div><p className="eyebrow">SMART MATCHES</p><h2>Recommended for you</h2></div><button className="link-button" onClick={() => setScreen('search')}>View all <ChevronRight size={16} /></button></section><div className="rides-grid">{rides.length ? rides.map((ride) => <RideCard key={ride._id} ride={ride} onJoin={join} />) : <EmptyRides onCreate={() => setShowCreate(true)} />}</div></> : <TripsView screen={screen} rides={rides} />}</main>{showCreate && <CreateRide onClose={() => setShowCreate(false)} onCreated={() => { setShowCreate(false); load(); }} />}</div> }

function RideCard({ ride, onJoin }) { return <article className="ride-card"><div className="ride-card-top"><span className="match-pill"><Sparkles size={13} />{ride.matchScore ?? (ride.availableSeats > 2 ? 92 : 84)}% Match</span><button className="more-button">•••</button></div><div className="route"><div className="route-line"><span></span><i></i><span></span></div><div><b>{ride.source}</b><small>{ride.departureTime}</small><b>{ride.destination}</b></div></div><div className="ride-meta"><span><Clock3 size={15} />{ride.estimatedDuration || 35} min</span><span><CarFront size={15} />{ride.vehicle?.model || 'Campus ride'}</span></div><div className="ride-footer"><div className="driver"><div className="avatar small">{ride.creator?.name?.slice(0, 2).toUpperCase() || 'CC'}</div><span><b>{ride.creator?.name || 'Verified student'}</b><small>★ {ride.creator?.rating?.toFixed?.(1) || '4.8'} rating</small></span></div><button className="join-button" disabled={!ride.availableSeats} onClick={() => onJoin(ride._id)}>{ride.availableSeats ? `${ride.availableSeats} seats` : 'Full'}</button></div></article> }
function EmptyRides({ onCreate }) { return <div className="empty-state"><div className="empty-icon"><MapPin size={25} /></div><h3>No rides on this route yet</h3><p>Be the person who gets the campus moving. Offer a ride and find your people.</p><button className="primary-button" onClick={onCreate}><Plus size={17} />Offer a ride</button></div> }
function TripsView({ screen, rides }) { return <section className="subpage"><div className="welcome-row"><div><p className="eyebrow">YOUR JOURNEY</p><h1>{screen === 'trips' ? <>Your <em>trips.</em></> : <>Your <em>conversations.</em></>}</h1><p className="muted">{screen === 'trips' ? 'Everything you have planned, in one place.' : 'Ride-specific conversations will appear here.'}</p></div></div>{screen === 'trips' ? <div className="empty-state"><CalendarDays size={28} /><h3>Your trips will show up here</h3><p>Join a ride from your recommendations to start building your commute history.</p></div> : <div className="empty-state"><MessageCircle size={28} /><h3>No conversations yet</h3><p>Join a ride to unlock its private chat room.</p></div>}</section> }
function CreateRide({ onClose, onCreated }) { const [form, setForm] = useState({ source: '', destination: '', date: '', departureTime: '', maxPassengers: 2, vehicle: { model: '', type: 'Car', capacity: 4 }, notes: '' }); const [error, setError] = useState(''); const submit = async (e) => { e.preventDefault(); try { await request('/rides', { method: 'POST', body: JSON.stringify(form) }); onCreated(); } catch (err) { setError(err.message); } }; const update = (key, value) => setForm({ ...form, [key]: value }); return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><div><p className="eyebrow">OFFER A RIDE</p><h2>Make space for someone.</h2></div><button className="icon-button" onClick={onClose}><X /></button></div><form className="ride-form" onSubmit={submit}><label>Pickup location<input required value={form.source} onChange={(e) => update('source', e.target.value)} placeholder="e.g. Pune Station" /></label><label>Destination<input required value={form.destination} onChange={(e) => update('destination', e.target.value)} placeholder="e.g. PICT Campus" /></label><div className="form-grid"><label>Date<input type="date" required value={form.date} onChange={(e) => update('date', e.target.value)} /></label><label>Departure time<input type="time" required value={form.departureTime} onChange={(e) => update('departureTime', e.target.value)} /></label></div><div className="form-grid"><label>Seats you offer<input type="number" min="1" max="20" required value={form.maxPassengers} onChange={(e) => update('maxPassengers', Number(e.target.value))} /></label><label>Vehicle<input value={form.vehicle.model} onChange={(e) => update('vehicle', { ...form.vehicle, model: e.target.value })} placeholder="e.g. Honda City" /></label></div><label>Note for riders<textarea value={form.notes} onChange={(e) => update('notes', e.target.value)} placeholder="Anything helpful about your pickup spot?" /></label>{error && <div className="error-box">{error}</div>}<button className="primary-button full">Publish ride <ChevronRight size={18} /></button></form></div></div> }

createRoot(document.getElementById('root')).render(
  <BrowserRouter>
    <App />
  </BrowserRouter>
);
